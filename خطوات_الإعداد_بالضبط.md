# خطوات الإعداد بالضبط - Step by Step

## المشكلة
الـ backend يعمل لكن قاعدة البيانات غير متصلة → HTTP 500 error

## الحلول المتاحة

### الخيار 1: Railway (أسهل - موصى به) ⭐
### الخيار 2: PlanetScale (مجاني - سريع)
### الخيار 3: Supabase (PostgreSQL - مجاني)
### الخيار 4: Render (مجاني)
### الخيار 5: Aiven (مجاني)

---

# الخيار 1: Railway (أسهل طريقة) ⭐

---

## الخطوة 1: إنشاء قاعدة بيانات على Railway

### 1.1. اذهب إلى Railway
- افتح: https://railway.app
- اضغط "Login" أو "Sign Up"
- سجل دخول بحساب GitHub (أسهل)

### 1.2. أنشئ مشروع جديد
- اضغط "New Project"
- اختر "Deploy from GitHub repo" أو "Empty Project"
- اختر اسم للمشروع (مثلاً: `smc-database`)

### 1.3. أضف MySQL Database
- في المشروع، اضغط "New" أو "+"
- اختر "Database" → "Add MySQL"
- انتظر حتى يتم إنشاء قاعدة البيانات (1-2 دقيقة)

### 1.4. احصل على معلومات الاتصال
- اضغط على MySQL service
- اذهب إلى "Variables" tab
- ستجد:
  - `MYSQLHOST` (مثلاً: `containers-us-west-xxx.railway.app`)
  - `MYSQLUSER` (مثلاً: `root`)
  - `MYSQLPASSWORD` (كلمة مرور)
  - `MYSQLPORT` (مثلاً: `3306`)
  - `MYSQLDATABASE` (مثلاً: `railway`)

**انسخ هذه المعلومات - ستحتاجها في الخطوة التالية**

---

## الخطوة 2: إضافة Environment Variables في Vercel

### 2.1. اذهب إلى Vercel Dashboard
- افتح: https://vercel.com/dashboard
- اختر مشروعك (`smc-digital-suite`)

### 2.2. أضف Environment Variables
- اضغط "Settings" (من القائمة الجانبية)
- اضغط "Environment Variables" (من القائمة)
- اضغط "Add New"

### 2.3. أضف المتغيرات التالية (واحد تلو الآخر):

**المتغير 1:**
- **Name:** `DB_HOST`
- **Value:** قيمة `MYSQLHOST` من Railway (مثلاً: `containers-us-west-xxx.railway.app`)
- **Environment:** اختر "Production", "Preview", "Development" (كلهم)
- اضغط "Save"

**المتغير 2:**
- **Name:** `DB_USER`
- **Value:** قيمة `MYSQLUSER` من Railway (مثلاً: `root`)
- **Environment:** اختر "Production", "Preview", "Development" (كلهم)
- اضغط "Save"

**المتغير 3:**
- **Name:** `DB_PASSWORD`
- **Value:** قيمة `MYSQLPASSWORD` من Railway
- **Environment:** اختر "Production", "Preview", "Development" (كلهم)
- اضغط "Save"

**المتغير 4:**
- **Name:** `DB_NAME`
- **Value:** قيمة `MYSQLDATABASE` من Railway (مثلاً: `railway`)
- **Environment:** اختر "Production", "Preview", "Development" (كلهم)
- اضغط "Save"

**المتغير 5:**
- **Name:** `DB_PORT`
- **Value:** `3306`
- **Environment:** اختر "Production", "Preview", "Development" (كلهم)
- اضغط "Save"

---

## الخطوة 3: إنشاء الجداول في قاعدة البيانات

### 3.1. اتصل بقاعدة البيانات
- في Railway، اضغط على MySQL service
- اضغط "Connect" أو "Open MySQL"
- أو استخدم MySQL Workbench / DBeaver

### 3.2. نفذ ملف schema.sql
- افتح ملف `backend/schema.sql` في المشروع
- انسخ كل محتوى الملف
- الصقه في MySQL و نفذه (Execute)

**أو:**

- في Railway، اضغط على MySQL service
- اذهب إلى "Data" tab
- اضغط "Query" أو "SQL Editor"
- الصق محتوى `backend/schema.sql`
- اضغط "Run" أو "Execute"

---

## الخطوة 4: إعادة Deploy على Vercel

### 4.1. ارفع التغييرات على GitHub
```bash
git add .
git commit -m "Add database configuration"
git push
```

### 4.2. أو إعادة Deploy يدوياً
- في Vercel Dashboard → Deployments
- اضغط "..." بجانب آخر deployment
- اختر "Redeploy"

---

## الخطوة 5: التحقق من الحالة

### 5.1. افتح Health Check
- افتح: `https://your-site.vercel.app/api/health`
- يجب أن ترى:
  ```json
  {
    "status": "ok",
    "database": "connected"
  }
  ```

### 5.2. جرب حفظ قسم
- افتح Dashboard
- حاول إضافة قسم جديد
- يجب أن يعمل بدون أخطاء ✅

---

## إذا لم يعمل

### تحقق من:
1. ✅ Environment Variables موجودة في Vercel (الخطوة 2)
2. ✅ القيم صحيحة (نسختها من Railway)
3. ✅ الجداول تم إنشاؤها (الخطوة 3)
4. ✅ تم إعادة Deploy بعد إضافة Environment Variables

### افتح Browser Console (F12):
- افحص الأخطاء
- ستجد رسالة أوضح عن سبب المشكلة

---

## ملخص سريع

1. ✅ أنشئ MySQL على Railway
2. ✅ انسخ معلومات الاتصال
3. ✅ أضف Environment Variables في Vercel
4. ✅ أنشئ الجداول (نفذ schema.sql)
5. ✅ أعد Deploy
6. ✅ اختبر

---

**بعد هذه الخطوات، يجب أن يعمل كل شيء! 🚀**

