# خطوات الإعداد - بدائل Railway

## الخيار 2: PlanetScale (مجاني - سريع) ⭐⭐⭐

### المميزات:
- ✅ مجاني تماماً (حتى 5GB)
- ✅ سريع جداً
- ✅ سهل الإعداد
- ✅ MySQL متوافق 100%

### الخطوات:

#### 1. إنشاء حساب على PlanetScale
- اذهب إلى: https://planetscale.com
- اضغط "Sign up" (سجل بحساب GitHub)
- أنشئ حساب جديد

#### 2. إنشاء Database
- اضغط "New database"
- اختر "Create new database"
- اسم: `smc_dashboard`
- Region: اختر الأقرب لك (مثلاً: `us-east`)
- Plan: اختر "Free" (Hobby)
- اضغط "Create database"

#### 3. الحصول على معلومات الاتصال
- بعد الإنشاء، اضغط على Database
- اذهب إلى "Settings" → "Passwords"
- اضغط "New password"
- اسم: `vercel` (أو أي اسم)
- اضغط "Create password"
- **انسخ هذه المعلومات:**
  - `Host`: (مثلاً: `xxxxx.psdb.cloud`)
  - `Username`: (مثلاً: `xxxxx`)
  - `Password`: (كلمة المرور التي أنشأتها)
  - `Database`: (اسم الـ database، مثلاً: `smc_dashboard`)
  - `Port`: `3306`

#### 4. إنشاء الجداول
- في PlanetScale Dashboard
- اضغط على Database → "Console" tab
- افتح ملف `backend/schema.sql` من المشروع
- انسخ محتوى الملف (بدون السطر الأول `CREATE DATABASE...`)
- الصقه في Console
- اضغط "Run" أو "Execute"

#### 5. إضافة Environment Variables في Vercel
- اذهب إلى Vercel Dashboard → Settings → Environment Variables
- أضف:
  ```
  DB_HOST=xxxxx.psdb.cloud
  DB_USER=xxxxx
  DB_PASSWORD=your_password
  DB_NAME=smc_dashboard
  DB_PORT=3306
  ```
- Environment: Production, Preview, Development (كلهم)

#### 6. إعادة Deploy
- Vercel Dashboard → Deployments → Redeploy

---

## الخيار 3: Supabase (PostgreSQL - مجاني) ⭐⭐

### المميزات:
- ✅ مجاني تماماً (حتى 500MB)
- ✅ PostgreSQL (قاعدة بيانات قوية)
- ✅ API تلقائي
- ⚠️ يحتاج تعديل بسيط في الكود (PostgreSQL بدلاً من MySQL)

### الخطوات:

#### 1. إنشاء حساب على Supabase
- اذهب إلى: https://supabase.com
- اضغط "Start your project"
- سجل بحساب GitHub

#### 2. إنشاء Project
- اضغط "New Project"
- اسم: `smc-dashboard`
- Database Password: (اختر كلمة مرور قوية)
- Region: اختر الأقرب لك
- Plan: Free
- اضغط "Create new project"
- انتظر حتى يتم الإنشاء (2-3 دقائق)

#### 3. الحصول على معلومات الاتصال
- بعد الإنشاء، اذهب إلى "Settings" → "Database"
- في قسم "Connection string" → "URI"
- **انسخ Connection String:**
  ```
  postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres
  ```
- أو استخدم:
  - `Host`: `db.xxxxx.supabase.co`
  - `User`: `postgres`
  - `Password`: (كلمة المرور التي اخترتها)
  - `Database`: `postgres`
  - `Port`: `5432`

#### 4. تعديل الكود (مهم!)
Supabase يستخدم PostgreSQL، تحتاج إلى:
- تثبيت `pg` بدلاً من `mysql2`:
  ```bash
  cd backend
  npm install pg
  ```
- تعديل `backend/database.js` لاستخدام PostgreSQL

**أو استخدم MySQL على Supabase:**
- Supabase يدعم MySQL أيضاً (في بعض الخطط)

#### 5. إضافة Environment Variables في Vercel
```
DB_HOST=db.xxxxx.supabase.co
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=postgres
DB_PORT=5432
```

---

## الخيار 4: Render (مجاني)

### المميزات:
- ✅ مجاني (مع قيود)
- ✅ MySQL متوافق
- ✅ سهل الإعداد

### الخطوات:

#### 1. إنشاء حساب على Render
- اذهب إلى: https://render.com
- اضغط "Get Started for Free"
- سجل بحساب GitHub

#### 2. إنشاء MySQL Database
- Dashboard → "New +" → "PostgreSQL" (أو MySQL إذا متاح)
- اسم: `smc-dashboard`
- Database: `smc_dashboard`
- User: `smc_user`
- Region: اختر الأقرب
- Plan: Free
- اضغط "Create Database"

#### 3. الحصول على معلومات الاتصال
- بعد الإنشاء، اضغط على Database
- في "Connections" tab
- **انسخ:**
  - `Host`
  - `Port`
  - `Database`
  - `User`
  - `Password`

#### 4. إضافة Environment Variables في Vercel
```
DB_HOST=your-host.onrender.com
DB_USER=your_user
DB_PASSWORD=your_password
DB_NAME=smc_dashboard
DB_PORT=5432
```

---

## الخيار 5: Aiven (مجاني)

### المميزات:
- ✅ مجاني (30 يوم تجريبي)
- ✅ MySQL متوافق
- ✅ سهل الإعداد

### الخطوات:

#### 1. إنشاء حساب على Aiven
- اذهب إلى: https://aiven.io
- اضغط "Start Free Trial"
- سجل حساب جديد

#### 2. إنشاء MySQL Service
- Dashboard → "Create service"
- Service type: MySQL
- Cloud provider: اختر (AWS, GCP, Azure)
- Region: اختر الأقرب
- Plan: Startup-4 (Free tier)
- اضغط "Create service"

#### 3. الحصول على معلومات الاتصال
- بعد الإنشاء، اضغط على Service
- في "Overview" → "Connection information"
- **انسخ:**
  - `Host`
  - `Port`
  - `Database`
  - `User`
  - `Password`

#### 4. إضافة Environment Variables في Vercel
```
DB_HOST=your-host.aivencloud.com
DB_USER=avnadmin
DB_PASSWORD=your_password
DB_NAME=defaultdb
DB_PORT=3306
```

---

## مقارنة سريعة

| الخيار | السعر | الصعوبة | السرعة | التوصية |
|--------|-------|---------|--------|---------|
| **Railway** | مجاني | ⭐ سهل | ⭐⭐⭐ سريع | ⭐⭐⭐⭐⭐ |
| **PlanetScale** | مجاني | ⭐ سهل | ⭐⭐⭐⭐⭐ سريع جداً | ⭐⭐⭐⭐⭐ |
| **Supabase** | مجاني | ⭐⭐ متوسط | ⭐⭐⭐ سريع | ⭐⭐⭐ (PostgreSQL) |
| **Render** | مجاني | ⭐⭐ متوسط | ⭐⭐ متوسط | ⭐⭐⭐ |
| **Aiven** | تجريبي | ⭐⭐ متوسط | ⭐⭐⭐ سريع | ⭐⭐ |

---

## التوصية النهائية

### أفضل خيار: **PlanetScale** ⭐⭐⭐⭐⭐
- مجاني تماماً
- سريع جداً
- MySQL متوافق 100%
- سهل الإعداد
- لا يحتاج تعديل في الكود

### ثاني أفضل: **Railway** ⭐⭐⭐⭐
- مجاني
- سهل جداً
- MySQL متوافق
- سريع

---

## بعد اختيار أي خيار:

1. ✅ أنشئ قاعدة البيانات
2. ✅ انسخ معلومات الاتصال
3. ✅ أضف Environment Variables في Vercel
4. ✅ أنشئ الجداول (نفذ schema.sql)
5. ✅ أعد Deploy
6. ✅ اختبر

---

**ملاحظة:** جميع الخيارات مجانية للتجربة. اختر الأسهل لك! 🚀

